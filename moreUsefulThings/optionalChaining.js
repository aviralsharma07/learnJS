const user = {
    firstName: "Nakesh",
    // address: {houseNumber: 24}
};

console.log(user.firstName);
console.log(user.address?.houseNumber);
// Question Mark Checks if the Object exist and access its property only if does. 